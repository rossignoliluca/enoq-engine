# runtime/pipeline

Runtime pipeline execution.

---

## Files

| File | Purpose |
|------|---------|
| `pipeline.ts` | Main `enoq()` function (DEPRECATED - use `enoqCore()`) |
| `l2_execution.ts` | L2 layer execution logic |

---

## Note

`enoq()` is deprecated. Use `enoqCore()` from `core/pipeline/orchestrator.ts`.

The runtime pipeline is delegated by core orchestrator for middle processing.
